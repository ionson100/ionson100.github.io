export const SimpleText = (text) => {
    return (<span style={{paddingLeft: 20}}>{text}</span>)
}
export const SimpleTextRed = (text) => {
    return (<span style={{paddingLeft: 20,color:"red"}}>{text}</span>)
}